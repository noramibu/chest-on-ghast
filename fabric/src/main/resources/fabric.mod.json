{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "license": "${mod_license}",
  "authors": [
    "${mod_authors}"
  ],
  "contact": {
    "sources": "${mod_repository}"
  },
  "icon": "assets/chest-on-ghast/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "me.noramibu.ChestOnGhastFabric"
    ]
  },
  "mixins": [
    "chest-on-ghast.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader}",
    "minecraft": "${minecraft_compat_min}.x",
    "java": "${java_version}.x",
    "fabric-api": "*"
  }
}
